// Debug yapılandırması. Yerel backend yok — Debug de Release gibi canlı
// backend'e (Vercel) bağlanır. Yerel bir sunucuya bakmak istersen:
//   API_BASE_URL = http:/$()/localhost:3000
// ("//" xcconfig'de yorum başlattığı için URL'de $() hilesi gerekir.)
API_BASE_URL = https:/$()/backend-ruby-xi.vercel.app
